import { NavLink } from "react-router-dom";

export default function Sidebar() {
  return (
    <aside className="w-64 bg-gray-900 text-white min-h-screen p-4">
      <h2 className="text-xl font-semibold mb-6">IT Manager</h2>

      <nav className="space-y-2">
        <NavLink to="/" className="block p-2 rounded hover:bg-gray-700">
          Dashboard
        </NavLink>
        <NavLink to="/assets" className="block p-2 rounded hover:bg-gray-700">
          Inventario
        </NavLink>
        <NavLink to="/tickets" className="block p-2 rounded hover:bg-gray-700">
          Tickets
        </NavLink>
        <NavLink to="/reports" className="block p-2 rounded hover:bg-gray-700">
          Reportes
        </NavLink>
      </nav>
    </aside>
  );
}
